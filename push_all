#!/usr/bin/env bash
set -e
cd /home/dev

git add -A

if git diff --cached --quiet; then
  echo "No changes to commit."
  exit 0
fi

msg="${1:-auto push $(date '+%Y-%m-%d %H:%M:%S')}"
git commit -m "$msg"
git push
